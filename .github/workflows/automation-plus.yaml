name: Automatically register issues in kanban

on:
  issues:
    types: [opened]

jobs:
  register issues in kanban:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.7.1
        with:
          project: 開発
          column: やる
          repo-token: ${{ secrets.GITHUB_TOKEN }}
